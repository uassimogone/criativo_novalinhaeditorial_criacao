name: Teste do Gerador

on:
  workflow_dispatch:

jobs:
  teste:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verificar estrutura
        run: echo "Repositorio de criacao inicializado com sucesso."
